"""台股市場資料爬蟲套件。

三個資料源各自一個模組，都遵循同一組介面：

    fetch_*()  只負責 I/O（HTTP 或瀏覽器），回傳未經處理的原始素材
    parse()    純函式，原始素材進、DataFrame 出，可用離線樣本測試
    run()      串起 fetch → parse → storage，處理迴圈與錯誤

把 I/O 與解析分開是這個專案的核心設計：解析邏輯不需要網路或瀏覽器就能測試。
"""
